/**
 * UserRole enum represents the roles of users.
 * It enumerates various roles such as MEMBER, ADMIN, and JURY.
 */
export enum UserRole {
  MEMBER = "MEMBER",
  ADMIN = "ADMIN",
  JURY = "JURY"
}
